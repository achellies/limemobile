<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Plugin.NoTitleBar" parent="@android:style/Theme.NoTitleBar"></style>
    <style name="Theme.Plugin.Translucent.NoTitleBar" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="Theme.Plugin.Dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
    </style>

</resources>